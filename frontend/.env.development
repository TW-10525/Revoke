# Development environment - uses Vite proxy
# The proxy in vite.config.js rewrites /api/* to http://localhost:8000/*
VITE_API_URL=/api

# Backend URL for Vite proxy (defaults to localhost:8000 if not set)
# The proxy runs on your local machine, so localhost:8000 works for both
# local access and dev tunnel access
# VITE_BACKEND_URL=http://localhost:8000
